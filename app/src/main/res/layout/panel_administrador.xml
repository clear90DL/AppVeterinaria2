<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".FragmentoAdministrador.InicioAdministrador">

    <!-- Contenido principal -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Toolbar -->
        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar1"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="#2196F3"
            android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
            app:title="Panel Administrador"
            app:titleTextColor="@android:color/white" />

        <!-- AQUÍ VA EL SWIPE REFRESH LAYOUT -->
        <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
            android:id="@+id/swipeRefresh"
            android:layout_width="match_parent"
            android:layout_height="match_parent">
            <!-- Contenedor dinámico para las vistas -->
            <FrameLayout
                android:id="@+id/contenedorVistas"
                android:layout_width="match_parent"
                android:layout_height="match_parent">
                <!-- El contenido de inicio_administrador.xml se cargará aquí automáticamente -->
                <include layout="@layout/inicio_administrador" />
            </FrameLayout>
        </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

    </LinearLayout>

    <!-- Panel lateral -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigationView"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        app:itemIconTint="@null"
        app:headerLayout="@layout/nav_header"
        app:menu="@layout/menu_lateral_administrador" />

</androidx.drawerlayout.widget.DrawerLayout>